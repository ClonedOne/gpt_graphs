python
Copy code
ranks = {0: [0], 1: [8, 5], 2: [3, 1], 3: [6, 4, 7], 4: [9], 5: [2]}

for rank_number in range(1, len(ranks)):
    current_rank = ranks[rank_number]
    for i in range(len(current_rank)):
        for j in range(i + 1, len(current_rank)):
            crossings = 0
            for e1, (u1, v1) in enumerate(edges):
                for e2, (u2, v2) in enumerate(edges):
                    if u1 in current_rank[i:j+1] and v1 in current_rank[:i+1]:
                        if u2 in current_rank[i:j+1] and v2 in current_rank[:i+1]:
                            if current_rank.index(u1) > current_rank.index(u2) and current_rank.index(v1) < current_rank.index(v2):
                                crossings += 1
            if crossings == 0:
                current_rank[i:j+1] = sorted(current_rank[i:j+1])
                break

ranks